signingConfig signingConfigs。
别名(libs.安卓插件插件{android。应用)插件。安卓.应用)
别名(libs.插件.科特林.安卓)插件。科特林.安卓)
}

安卓{
命名空间'com.stand.sounder_template''com.stand.sounder_template'
compileSdk3636

DefaultConfig{
应用程序ID"COM.stand.sounder.爱莉"com.站立.发声器.模板"
minSdk2323
targetSdk3636
版本码11
VersionName”1.0”"1.0"

testInstrumentationRunner"androidx.test.runner.androidJUnitRunner""androidx.test.runner.androidJUnitRunner"
}

signingConfigs{
发布{
storeFile文件(“公共。密钥库”)"public.keystore")
storePassword“public”"公共"
keyAlias“public”"公共"
keyPassword“public”"公共"
        }
    }

buildTypes{
发布{
minifyEnabled假假
proguardFiles getDefaultProguardFile(proguard-android-timestize。文本')，'程序规则。赞成''程序-安卓-优化。 文本'), 'ProGuard规则。赞成'
signingConfig signingConfigs。释放释放
        }
    }

androidTestImplementation库。
sourceCompatibilityJavaVersion。版本_17版本_17
targetCompatibility JavaVersion.version_17VERSION_17
}

kotlinOptions{
jvmTarget="17""17"
    }
}

相依性{
实现libs.androidx.核心.ktxandroidx.核心.KTX
实现libs.androidx.appcompatandroidx.AppCompat
实现libs.材料材料
testImplementation库.junitjunit
androidTestImplementation库。androidx。junitandroidx.JUnit
androidTestImplementation库。androidx。浓缩咖啡.coreandroidx.浓缩咖啡.核心
}
